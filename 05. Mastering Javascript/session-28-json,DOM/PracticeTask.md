# PracticeQuestion

1. Create HTML code add 3 pragraphs, add 2 buttons red and blue.
    - if user clicks on red change colors of all 3 para to red
    - if user clicks on blue change colors of all 3 para to blue.

2. Add Border to 3 input tags usiong Button click

3. create 4 divs with class card, may be you have more divs with diffrent classess. Now write a logic to count div and print how many divs having card class.
    - display count in browser console.
    - no css required here

## Notes

- innerText not setting text on hiddenElement (slower)
- textContext can set text on Hidden Elements (fast)

- innerText & textContent now allowing HTML tag inside text if you add its shows as text only.
- innerHTML will resolve above Problem.
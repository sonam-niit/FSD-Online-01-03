# Practice Task

1. Write a function that checks whether a number is even or odd.
     - function take 1 number type parameter
     - which returns true if number is even (return type boolean)

2. Create an interface Employee and write some objects of employee.
    - employee can have id, name, salary, deplartment
    - give required datatypes
    - create seperate function named display which takes employee type object and print values

    function display(emp:Employee){
        //code to print
    }

    let em1:Employee= {}
    let em2:Employee= {}
    let em3:Employee= {}

    display(em1)
    display(em2)
    display(em3)

3. Create Type Alias for a Product Object
    - id, name, price
    - create objects and print

4. Create Type alias
    - variable name result can accept 2 types of value
    - either string or number
    - if i say result = "Pass" // this is also correct
    - if i say result = 95 // this is also correct